## 2026-03-26
- L2 Reborn VIP cron remains safe/working. Core script `/home/user/OpenClaw/scripts/l2reborn-vote.js` is model-agnostic and works under GPT-5.4. Cron delivery was set to Discord channel `ai-reports` id `1486653715405602927` to avoid multi-channel routing errors.
- Mission Control stability diagnosis: major issue was that `/home/user/openclaw-mission-control` was running in dev mode (`next dev`) on port 3000. Switched startup in `/home/user/start-openclaw.sh` to production mode (`npm run start -- -H 127.0.0.1 -p 3000`) and verified `/dashboard` and `/api/system` respond.
- User wants Mission Control to feel like one seamless workspace connected to Telegram/Discord/webchat memory/actions, and wants cron/reporting to behave like one body across channels.
- Backup created before further Mission Control work at `/home/user/OpenClaw/backups/20260326-113307` containing `openclaw-mission-control/`, `start-openclaw.sh`, `l2reborn-vote.js`, and `cron-list.json`.
- Fanout/reporting investigation: added test route `src/app/api/fanout/test/route.ts` in Mission Control to try multi-channel fanout. Route is reachable, but runtime served stale compiled handler behavior even after rebuild/restart attempts (source uses `key`, runtime still complained about old `sessionKey`). Strong diagnosis: stale `.next` build artifact / stale compiled route issue. Next safe step is hard clean rebuild: stop MC, remove `.next`, rebuild from scratch, start one fresh prod instance, retest.
- Set up Discord bot integration in OpenClaw for SocialHub.
- Discord bot name changed to "Johnny's gaylord bot".
- Discord server: UrekMazino's server (id: 1084829267340316692); general channel id: 1084829267935895615.
- Friends in server have chat-only access; owner controls bot via Telegram/OpenClaw.
- Mission Control/OpenClaw dashboard screenshot capability now works via Playwright headless Chromium after installing browser deps.
- Verified user's actual Mission Control app is a Next.js app on http://localhost:3000, while OpenClaw gateway dashboard runs on http://127.0.0.1:18789/.
- User wants minimal replies to reduce token usage: only reply when necessary or essential.
- Discussed automating L2Reborn VIP 12h ticket claim; likely solution is headless Playwright + 2captcha/Turnstile solver, since cookies expire and captcha blocks full automation.
- Estimated 2captcha cost for 2 solves/day is only a few cents per month.
- Discord bot token was reset on 2026-03-26 and updated in ~/.openclaw/openclaw.json.
- Later regression found: Mission Control had been switched back to the old app at `/home/user/OpenClaw/mission-control` running `next dev` on port 3000, causing lag/crash behavior to return.
- Reapplied the stable fix by killing the old dev instance and starting the correct app `/home/user/openclaw-mission-control` in production mode (`npm run start -- -H 127.0.0.1 -p 3000`); verified `/dashboard` returned HTTP 200.
- Important continuity note: this session cannot automatically see work done in other provider chats/surfaces unless it is present in the current chat, files, or saved memory.
- Session-status verification on 2026-03-26: the active chat model is `anthropic/claude-sonnet-4-6`; `openai-codex/gpt-5.4` is configured only as fallback. If Mission Control/UI shows GPT-5.4 while the user chose Claude, treat it as a display/status bug rather than the active responder model changing.
